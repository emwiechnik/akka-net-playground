akka {
	loglevel = WARNING

	actor {
		provider = "Akka.Remote.RemoteActorRefProvider, Akka.Remote"
		debug {
			receive = on
			autoreceive = on
			lifecycle = on
			event-stream = on
			unhandled = on
		}
		deployment {
			/PlaybackActor/PlaybackStatisticsActor {
				remote = "akka.tcp://MovieStreamingActorRemoteSystem@localhost:8091"
			}
		}
	}

	remote {
#		helios.tcp {
#			transport.tcp {
#				transport-class = "Akka.Remote.Transport.Helios.HeliosTcpTransport, Akka.Remote"
#				transport-protocol = tcp
#				port = 0
#				hostname = "localhost"
#			}
#		}
		dot-netty.tcp {
			port = 0
			hostname = "localhost"
		}
	}
}